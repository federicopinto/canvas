{
  "name": "canvas-class-diagram",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:perf": "node tests/generate-report.js",
    "report": "npm run build && npm run test:perf"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "vite": "^5.0.0"
  }
}
